# api_gateway_url
VITE_API_BASE_URL=
# cognito_domain (Hosted-UI domain)
VITE_COGNITO_DOMAIN=
# cognito_user_pool_client_id
VITE_COGNITO_CLIENT_ID=
# cognito_user_pool_id — not used by the Hosted-UI flow; kept so all values live here
VITE_COGNITO_USER_POOL_ID=
# api_gateway_url + "/callback" (Cognito redirect target; must end in /callback, NOT /auth/callback)
VITE_COGNITO_REDIRECT_URI=
VITE_COGNITO_SCOPES=
# frontend_website_url + "/auth/callback" (where API GW lands tokens; currently informational)
VITE_FRONTEND_CALLBACK_URL=